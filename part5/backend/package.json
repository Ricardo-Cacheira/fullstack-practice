{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Ricardo Cacheira",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env  NODE_ENV=test node --test",
    "lint": "eslint .",
    "start:test": "cross-env NODE_ENV=test node --watch index.js",
    "testBlog": "cross-env NODE_ENV=test npm run test -- tests/blog_api.test.js",
    "only": "npm test -- --test-only",
    "testUser": "cross-env NODE_ENV=test npm run test -- tests/user_api.test.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.2.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.8.0",
    "eslint": "^10.0.0",
    "globals": "^17.3.0",
    "supertest": "^7.2.2"
  }
}
