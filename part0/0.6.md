```mermaid
sequenceDiagram
    participant browser
    participant server
    
    Note right of browser: Browser adds new note to local array
    Note right of browser: Browser clears existing list and recreates it with the updated entry
    Note right of browser: Browser sends the new note to be saved on the server
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: Response 201
    deactivate server
```