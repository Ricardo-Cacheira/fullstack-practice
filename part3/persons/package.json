{
  "name": "persons",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Ricardo Cacheira",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/persons",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"updatePhonebook 3.9-3.11\" && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.1.6",
    "morgan": "^1.10.1"
  }
}
